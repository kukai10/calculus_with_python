import math

def perpendicular_slope(slope, equation=None, point=None, derivative_equation=None):
    if equation == point == derivative_equation == None:
        return -1/slope
    else:
        pass
        # find slope at a point given an equation or the derivative of the equation
